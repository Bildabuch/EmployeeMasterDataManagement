/**
 * The base URL for API requests.
 *
 * This constant defines the base URL used as the starting point for all API calls
 * within the application. By default, it points to a local development server
 * running on port 8080.
 *
 * Example:
 * - An API request to `/api/employees` will resolve to `http://localhost:8080/api/employees`.
 */
export const baseUrl = "http://localhost:8080";